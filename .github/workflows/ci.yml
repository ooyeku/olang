name: CI

# PAUSED: automatic push/PR triggers are disabled while GitHub Actions is
# blocked on account billing (jobs were refused before starting, showing a
# red X on every push without running). To restore once billing is fixed,
# re-add the `push`/`pull_request` triggers below (or `git revert` the
# commit that paused them). `workflow_dispatch` still allows a manual run.
on:
  workflow_dispatch:
  # push:
  #   branches: [main]
  # pull_request:

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

jobs:
  test:
    name: Test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo registry and build
        uses: Swatinem/rust-cache@v2

      # reqwest uses native-tls, which needs OpenSSL headers on Linux
      - name: Install OpenSSL (Linux)
        if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install -y pkg-config libssl-dev

      - name: Build
        run: cargo build --workspace --all-targets --verbose

      # Runs the whole suite, including the two suites that keep the bytecode
      # tier honest:
      #   bytecode_differential_test — the VM must match the interpreter
      #   bytecode_tier_test         — promotion must not change results
      - name: Test
        run: cargo test --workspace --verbose

  lint:
    name: Lint
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Cache cargo registry and build
        uses: Swatinem/rust-cache@v2

      - name: Install OpenSSL
        run: sudo apt-get update && sudo apt-get install -y pkg-config libssl-dev

      - name: Formatting
        run: cargo fmt --all -- --check

      - name: Clippy
        run: cargo clippy --workspace --all-targets -- -D warnings
