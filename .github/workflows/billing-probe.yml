# Temporary: one echo on the cheapest runner to confirm Actions is no
# longer billing-blocked, before any real trigger is restored. Deleted
# in the same session that adds it.
name: billing-probe
on:
  workflow_dispatch:
jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - run: echo "actions are unblocked"
