{
  "name": "olang-website",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "sync-brand": "bun scripts/sync-brand.mjs",
    "sync-playground": "bun scripts/sync-playground.mjs",
    "dev": "bun run sync-brand && bun run sync-playground && vite dev",
    "build": "bun run sync-brand && bun run sync-playground && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@sveltejs/adapter-static": "^3.0.0",
    "@sveltejs/kit": "^2.0.0",
    "@sveltejs/vite-plugin-svelte": "^5.0.0",
    "marked": "^12.0.0",
    "shiki": "^1.22.0",
    "svelte": "^5.0.0",
    "vite": "^6.0.0"
  }
}
