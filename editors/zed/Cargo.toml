[package]
name = "zed-olang"
version = "0.1.0"
edition = "2021"
publish = false

# Standalone: not a member of the olang workspace above.
[workspace]

[lib]
crate-type = ["cdylib"]

[dependencies]
zed_extension_api = "0.7"
