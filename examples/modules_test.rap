// Test module functionality in Olang

// Export some values
export PI = 42;
export greet = (name) => name;

println("Module exports:");
println(PI);
println(greet("World"));

println("Module system working!"); 