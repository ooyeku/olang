# Roadmap

The plan of record. Item statuses progress from **planned** to **in
progress** to **landed**; nothing is removed, so the table is also the
history of what was decided. The campaigns that built the current
language — semantics, the data stack, capabilities on every tier, the
tier boundary, the engine additions — are complete; their record lives
in the CHANGELOG and this file's git history.

Version 1.0.0 ships when the project's owner decides it ships. Nothing
in this document is a countdown.

The workstreams below come from a deliberate audit for underdeveloped
ground (2026-08-26): each item cites what was actually observed, so
the work is tightening real edges, not speculation.

## W1 — text is made of graphemes

String operations index by codepoint, which corrupts what users see as
characters: `str.reverse("👋🏽ab")` tears the skin-tone modifier off its
emoji, `str.substring` and `str.char_at` cut clusters mid-character,
and `len` counts 👋🏽 as two. Decide the model (grapheme-aware default,
or documented codepoint semantics plus a grapheme API), implement it
uniformly across `len`/`str.*`, and pin it with tests over the unicode
segmentation cases.

| Item | Observed | Status |
|---|---|---|
| Grapheme decision + implementation | `str.reverse` corrupts emoji; `substring`/`char_at` split clusters | **landed** — codepoints stay the documented indexing unit (stated in language.md's "Codepoints and graphemes"); `str.graphemes(s)` is the visible-character view (UAX #29 extended clusters as a list, composing with len/index/slice/join); `str.reverse` reverses by grapheme so skin tones, ZWJ families, accents, and flags stay whole; pinned by tests/graphemes_test.rs (ZWJ, modifiers, combining marks, regional indicators, Hangul jamo, CRLF) |

## W2 — numbers read back in

The runtime prints floats the parser cannot read: `1e21` and
`9.999999999999999e-10` come out of `to_string`, while `1e20` in
source is a parse error — printed values are not valid literals.
Scientific-notation literals are simply missing from the grammar.
Alongside: float semantics are trapping (`1.0 / 0.0` and
`math.sqrt(-1.0)` error rather than produce inf/NaN) — a defensible
stance that must be stated as the contract, and JSON silently converts
big integers to floats (`99999999999999999999999999` → `1e26`).

| Item | Observed | Status |
|---|---|---|
| Scientific-notation literals | `1e20` fails to parse; printed floats aren't source | **landed** — full float literal syntax (`1e20`, `2.5e-3`, `1E+6`, underscores); print/parse round-trip pinned by a bit-for-bit proptest over arbitrary f64s |
| Trapping-float contract documented | division by zero and `sqrt(-1)` trap by design | **landed** — language.md and stability.md state the contract: would-be-`NaN` operations trap; the overflow edge below is recorded rather than papered over |
| Float overflow yields `inf` | `1e308 * 10.0` prints `inf`, and `inf - inf` then prints `NaN` — the trapping stance has a gap (found while documenting it) | **landed** — the ruling: a Float is always finite. Overflow raises on all three tiers (`Float overflow in multiplication (the result is not a finite number)`; the bytecode fast path leaves a non-finite result to the slow path, the JIT guards every float result with one compare and deopts to the same error), `math.*` functions error instead of returning `inf`, `str.parse_float`/`to_float` report "out of range", and a literal beyond f64 is a syntax error. The JIT cost on the n-body benchmark (2,000,000 steps, all float arithmetic): 0.63 s → 0.65 s. The data stack's Series kernels stay IEEE for throughput and are documented as the one place `inf` can appear (ods.md) |
| Int/Int series division truncates silently | building examples/data-processing/climate: OWID GDP columns infer as Int, so `(gdp - gdp_then) / gdp_then` on Series truncated every growth ratio to 0 — "54 countries decoupled" first read "0 countries", with no error and no hint. Consistent with the scalar language (`7 / 2` is `3`), which is exactly why it hides: in data work ratios are the norm and a truncating `/` between measure columns is almost never what the analysis means | **landed** — the ruling: Series division is true division. `/` between two Int Series, or an Int Series and an Int, yields a Float Series (`ods.cast(a / b, "Int")` for the integer quotient); `+ - *` stay Int and checked; zero still raises. Stated in ods.md ("Division is true division") and pitfalls.md beside the scalar rule; the climate shape `(now - then) / then` is the pinned regression (tests/w2_rulings_test.rs) |
| JSON numeric fidelity | oversized ints silently become floats | **landed** — reject-not-convert: integers within i64 arrive exactly, decimals take the IEEE reading, an integer outside i64 (either sign, including u64-range) or an overflowing float text is an `Err` naming the value; serde's arbitrary_precision keeps the source text so the JSON grammar decides the reading |

## W3 — errors that teach

The error surface is strong in places (index bounds, integer overflow
pointing at bigint) and embarrassing in others, all observed live:

| Item | Observed | Status |
|---|---|---|
| C-style block hint | `if x > 1 { ... }` → "expected an operator or a function call", no mention of `=>` | **landed** — brace-after-condition suggests the exact rewrite (`if x > 1 => {`) and explains that only `if`/`else` take the arrow |
| Unclosed-delimiter tracking | unclosed `[` reports "expected an operator" on the *next* line, never "opened at line N" | **landed** — a string- and comment-aware scan names the opener: "Unclosed `(` opened at line 2, column 13" |
| Module-not-found hint | `use geometry` → "This appears to be a system-level error" | **landed** — the REPL now shows the full search (paths, shelf, near-miss suggestions incl. shelf libraries) and the help names `use lib.<name>` and `otc lib list` |
| Non-function call | `x(1)` → "Cannot call non-function value" — no name, no type | **landed** — "'x' is an Int, not a function"; Maps/Lists get an indexing hint, everything else a shadowing hint |

## W4 — the REPL under stress

An unbalanced `(` traps the session in continuation: every subsequent
line — including `:help` and commands — is swallowed into the buffer,
nothing indicates what is unbalanced, and only Ctrl-C escapes. The
session transcript that surfaced the bridge bug shows a user hitting
this six times in a row.

| Item | Observed | Status |
|---|---|---|
| Continuation escape + indicator | commands swallowed; no unbalance display; Ctrl-C the only exit | **landed** — the continuation prompt wears what is open (`(( ...> `), balancing the input evaluates it immediately (no `:end` needed), `:cancel` abandons, commands and `quit` mid-continuation warn and name the unclosed delimiter instead of vanishing; `:ml` now actually exists (deliberate multi-statement entry, `:end` to run) and `:ml`/`:end`/`:cancel` have true `:help` entries |
| Session-state differential harness | three session-poisoning bugs in one week (help cache, bridge landscape, meta.eval tier), all user-found | **landed** — seeded generated action sequences (redefine-after-promotion, hot loops, meta.eval, collections) driven through the real binary and pinned two ways: every step equal under the tiered REPL and the `--no-ovm` interpreter oracle, and every step equal on fresh replay of its prefix. First run caught a real one: maps/structs printed in per-process hash order — Display and the REPL colorizer now print sorted by key, the language's canonical order |

## W5 — tools that do what they claim

| Item | Observed | Status |
|---|---|---|
| Test blocks are not inert | building the web SDK: `use` of a module (and any bundled/spliced program) *executed* its test blocks as ordinary statements — a loaded module's tests printed, mutated, and ran their asserts at import time | **landed** — test blocks parse everywhere, execute only under `olang test`; pinned with a runner-still-runs-them check |
| Re-exports lose their closures | web-sdk's index re-exported `action` from a module with private cells; the re-closing pass rebound it over the *aggregator's* scope — "Undefined variable: mount_actions" at first call | **landed** — `share use` re-exports keep the closure their own module gave them; pinned |
| Test asserts inside match arms | `assert_eq` is "Undefined variable" inside a match arm within a `test` block (minimal repro pinned); tests route around it by extracting first | **landed** — the five asserts are builtins with the statement form's exact raising semantics, so expression positions (match arms, lambdas) resolve them |
| `olang fmt` formats | `fn f( x ,y )=x+y` reported "all formatted (1 file scanned)" — the formatter normalizes nothing | **landed** — token respacer calibrated against the whole in-repo corpus (the shipped style is the spec); alignment before `=>` and trailing comments preserved; raw-parse identity gate (span-insensitive decl equality fixed the gate refusing any line shift); all 127 corpus files formatted and idempotent |
| LSP depth audit | feature-frozen for months; current depth unknown; the `:help` registry and doc pipeline make hover/signatures newly cheap | **landed** — protocol-level audit, then the robustness batch: user-decl hovers with /// docs, `share` declarations first-class, inlay hints, code actions (doc stub, `let mut` quick fix), workspace symbols, `use` completions; diagnostics gained the scoping pass (assignment-to-immutable now surfaces) and stopped flagging stdlib modules. Grammar + Zed highlighting rebuilt for a distinct olang identity (112-file corpus, zero parse errors); extension 0.3.0 |

## W6 — concurrency you can see into

| Item | Observed | Status |
|---|---|---|
| Blocked-channel visibility | `chan.recv` with no sender hangs the program forever, silently | shipped — the stall detector: every olang thread is censused, unbounded waits are parked sites, and an all-parked program aborts with a report naming each blocked site (`OLANG_STALL_ABORT=0` opts out) |
| Task/channel introspection | no way to ask what is running or blocked | shipped — `task.list()`, `task.parked()`, `chan.stat(c)` |
| Timeline reach | record/replay covers main-thread fs/net; db reads, channels, and workers run live and silently break the "clean replay is proof" property beyond a warning | planned |

## W7 — the engine keeps proving itself

| Item | Observed | Status |
|---|---|---|
| Runtime tier self-verification | tier agreement is proven by the suite, not by the running program; two "since it shipped" latent bugs this month argue for live evidence | shipped — `--verify-tiers <rate>` re-executes sampled native results (compiled calls and OSR regions, pure by whitelist construction) on the VM and aborts with a report on divergence; `:ovm` counts the session's verified calls |
| HTTP server hardening | never adversarially probed: malformed requests, oversized payloads, slow clients | shipped — explicit `serve` limits (`max_header_bytes`, `max_body_bytes`, `request_timeout_ms`), precise 400/408/413/431 answers, header-injection flattening; `tests/http_abuse_test.rs` is the gauntlet |
| Platform coverage | every gate runs on one macOS machine; `setup.sh` claims cross-platform | shipped — `dist/linux-verify.sh` runs the full suite and the examples harness in a Linux container (RELEASING.md gate list); architecture follows the host |

## W8 — what building the crimes workstream asked for

Every row below was observed while building
`examples/data-processing/crimes/` into a fourteen-stage,
three-model analysis (2026-08-30) — the largest olang program yet
written, and a deliberate audit of the data-science surface by use.

| Item | Observed | Status |
|---|---|---|
| Multi-series plotting | the calibration diagram needs its points *and* a y=x reference; the three models' F1-threshold curves belong on one chart with a legend; rates-with-intervals want grouped bars. None expressible — every `plot.*` chart is a single series, so cross-model comparisons live only in tables | planned — a multi-series form (shared axes, legend) and a histogram primitive; the crimes charts are the acceptance cases |
| A vector/matrix layer for numeric code | every kernel in `lib/ml.ol` — dot products, axpy updates, covariance, standardization — is a hand-written index loop, and the feature matrix is a list-of-columns convention each function re-documents; extraction from a frame repeats `ods.to_list(ods.cast(...))` per column | planned — `vec.dot/scale/add/sum` over Float lists (dedicated JIT targets), and `ods.to_matrix(frame, cols)` blessing the columns convention once |
| Error spans through promoted code | a division-by-zero inside `tree_train` reported its span as `main.ol:405 println("")` — the call stack named the right function but the wrong site, and finding the failing expression meant rebuilding the function inline with prints | shipped — the root cause was the emitter leaking one function's span rows into the next compile (a dependency inherited its caller's table, so errors pointed at another function's lines); fixed along with three trace losses found by the new suite: hot map/filter kernels dropped their trace entirely, bridged-lambda errors lost location crossing the boundary, and the interpreter itself recorded stacks after unwinding had popped the deep frames. tests/error_span_test.rs pins byte-identical reports across seven acceleration shapes |
| Template-literal ergonomics | backtick strings neither process escapes nor nest, and both facts surface only at runtime: progress bars printed a literal `\r` per frame, and report assembly needed four rounds of hoisting `let`s out of `${...}` | shipped — ruled: the semantics stay (templates process no escapes, do not nest), and the mistakes are caught early instead. `olang check` warns on an escape-looking `\n`/`\t`/`\r` inside a template, positioned at the exact backslash and read from the source spelling (`\\n` — the same two output characters — is the deliberate form and stays silent; escapes inside a `${...}`'s double-quoted strings are real syntax and exempt); the parse errors a backtick inside `${...}` produces now name the nesting rule and the bind-to-a-name fix. tests/template_lint_test.rs pins warnings, exemptions, both error paths, and runtime unchanged |
| Import shadowing | `use term` silently rebound `table` over the earlier `use lib.report { table }`; the failure surfaced at runtime as "len: argument must be a list" inside the wrong function | shipped — `use lib.report { table as md_table }` binds only the alias (share-use re-exports under it too; `as` stays contextual), and `olang check` warns on a wildcard `use` that shadows an earlier explicit import, naming both lines and the two fixes; tests/import_alias_test.rs pins runtime and warning both |
| `map_get` on a missing key | returns Unit, not a Result — `unwrap_or(map_get(q, "v"), d)` raises instead of defaulting, and the guard idiom (`map_has_key` first) must be known in advance | shipped — ruled: `map_get` keeps its shape and is documented as the raw read; `map_get_or(m, k, default)` (in the language since 0.69) is the blessed lookup-with-default, now beside `map_get` in the language reference. No Option type; absence is one rule — a missing key and a stored Unit both take the default |
| Derived group keys and splits | the year-month series needed a two-key `group_by` plus two `sort_by` calls; the train/test split is a hand-written every-5th-row scheme, and the report's own limitations section names the chronological leakage a forward-in-time split would fix | planned — a date-truncation/derived-key helper for `group_by`, and split utilities (deterministic holdout, forward-in-time) in ods or a stdlib `ml` module. The Int/Int series division row in W2 belongs to this same cluster |
| Optional parameters | `logistic_train_with`/`kmeans2_with` exist solely because there are no default arguments — every progress-aware function pays a duplicate-signature tax and the original name becomes a delegating stub | shipped — defaults and named arguments already parsed and half-worked; the row became three fixes: defaults now evaluate in the callee's scope with earlier parameters bound (they leaked the caller's environment — a live dynamic-scoping bug), defaulted functions promote and compile (they were blanket-rejected from the tier: a hot defaulted call was 650× slower; compiled call sites splice literal defaults, others take the interpreter-exact value path), and the semantics are documented with an eight-way differential battery. The `_with` stubs in crimes/lib/ml.ol are gone |
| Sole-owner map writes | the xlang wordfreq benchmark: `m = map_set(m, k, v)` clones the whole map per insert on both tiers even when the move fusion makes the argument sole-owner, so hot string-keyed counting is quadratic — the one benchmark olang cannot finish; `collections.table` is no rescue once the calling loop promotes (every put crosses the tier boundary converting the handle, the C7 T2 lane) | shipped — the fused `MapSetAssign` with the list writes' sole-owner discipline, VM and interpreter both, shadow-exact (compile-time decline for locals and known user functions, run-time re-check for late shadows); wordfreq DNF → 1.19 s. JIT lowering via a map ownership family remains available if profiles ever demand it |
| Approximate test assertions | the estimator test blocks reduce to `assert_eq(x > 0.5, true)` because there is no tolerance-based comparison — numeric tests are both weaker and less readable than they should be | shipped — `assert_close(actual, expected, tolerance, message?)` beside the existing asserts, everywhere-an-expression like them, Int/Float mixing, NaN fails, negative tolerance errors; the crimes estimator tests adopted it and tests/assert_close_test.rs pins the differential |

## W9 — what building open-track asked for

Every row below was observed while building open-track, a full-stack
issue tracker on the web SDK with a query language, a workflow
sandbox, attachments, and a CLI (2026-08-31) — the first large
program written *against* olang's packages from outside the
repository, which is why so many rows are about the seams between a
project and its dependencies. Rows are grouped by severity; within a
group the order is the order to take them. Tags name the surface:
`[olang]` the language and runtime, `[stdlib]` builtins and bundled
packages, `[otc]` the project tool, `[web-sdk]` the web framework.

### Wrong answers and divergence

| Item | Observed | Status |
|---|---|---|
| `[olang]` An imported name resolved to another module's import of the same name | `lib/rules.ol` did `use open_track_rules { check }` and wrapped it; `lib/app.ol` did `use lib.rules` plus `use validate { check }`. Called through app.ol, the wrapper ran *validate's* `check` (`Err(["expected a map, got String"])`); called from rules.ol's own test block or a probe file it ran the right one. Every three-file reduction resolved correctly — the trigger is in the full application's load order or module cache. Every unit test passed; it cost an afternoon. Workaround: never import a generic name by name across modules; qualify (`open_track_rules.check`) | **landed** — the module cache is keyed by the resolved file, never by the dotted name (two packages' `lib.util` were one cache entry; whichever loaded first answered for both), and the cycle detector keys the same way. tests/module_resolution_test.rs loads two packages exporting `check` through a wrapper module beside the consumer's own `lib.util`, in both orders |
| `[olang]` Dependency-internal `use lib.*` resolves against the consumer | a module *inside* a dependency (`web`'s `lib/view.ol` doing `use lib.state`) resolved `lib.state` against the consuming project's root. It works only when the module is already in the cache — when the package's `index.ol` imported it earlier in its own `use` list — so every consumer of the web package hit `Cannot find module 'lib.state'` until `index.ol` was reordered (committed as the workaround; the ordering constraint is invisible and will regress) | **landed** — a module's imports resolve against its own package first (nearest `olang.toml` or dependency root), before the entry project's root; the ordering constraint in web-sdk's index.ol is gone. Pinned with the dependency-with-nested-siblings case beside a consumer whose own `lib/b.ol` exports the same name |
| `[olang]` Pipeline-into-lambda diverges between native and wasm | `1 \|> ((x) => x + 1)` is a runtime error natively ("Pipeline right side must be a function call or function identifier") and runs fine in the browser build — open-track's client used it in three places for weeks before native tests met it in server code. Two engines, one program format, two answers | **landed** — the interpreter applies any callable expression on the right of `\|>` (`x \|> ((a) => a + 1)`, `x \|> steps[i]`), as the bytecode tier's desugaring always did; `x \|> f(a)` stays `f(x, a)`. Differential-pinned in tests/w9_language_test.rs; language.md states the asymmetry |
| `[olang]` A closure that references a later top-level binding fails at the click, and the raise takes the wasm instance down | an event handler registered early referenced a `cell` declared further down the file; native `olang test` passed (tests call functions, not the handler), and clicking in the running app crashed the page with `RuntimeError: unreachable`. `fn` declarations resolve forward references, which makes the `let` behavior feel inconsistent. The minimal native case (`let f = () => later` / `let later = 3` / `f()`) resolves correctly on 0.80.0, so the failure is in the bundled or wasm path | **landed** — reproduced in a browser session against the SDK and found to be a tier divergence, not a scoping rule: interpreted, a closure's free name resolves through the live scope chain, so a `let` declared after the closure is visible when it runs; with the tier on (always, in the browser), a function value the compiler declined ran on the *bridge* interpreter, which knew the program's functions but not its top-level `let`s. The bridge is now seeded with the host program's globals (an O(1) Arc handoff before each tier entry, reseeded only when the root map moves), and the three shapes — a lambda called through a compiled function, a named fn, a handler stored in a cell and dispatched — agree on both tiers. The raise-traps-the-instance half did not reproduce: three raises inside handlers came back as console errors with the page alive, so the event path's error marshalling stands |
| `[stdlib]` `to_string` on a String returns it quoted | `to_string("open")` is `"\"open\""` (length 6) — the show form, not the value. Generic code that stringifies a value from a map (query matching, key building, sort keys) silently grows quotes and stops comparing equal; open-track's query language returned zero matches for every field filter until this surfaced. The web SDK carries a private guard for it (`as_text` in `lib/html.ol`) | **landed** — `to_string(s)` is `s`; `show` and `to_string` agree on every value, containers still print their elements quoted, and `meta.lit` renders the literal form. stdlib.md and pitfalls.md rewritten; the old pin in tests/tier1_features_test.rs inverted |
| `[olang]` `col.any` with a closure is 100× slower under the bytecode tier than the loop it replaces | over 2,000 records: `col.any(xs, (a) => contains(include, a))` 138 ms tier-on vs 24 ms `--no-ovm`; the same as a for loop 1 ms tier-on. `col.all` and `filter`/`map` with the same lambda are fast, so it is `col.any`'s callback path specifically. Every `filter`-with-`col.any` pays it silently, and the browser cannot turn the tier off. open-track replaced it with loops on hot paths (query engine 21 ms → 4 ms) | **landed** — two causes: a VM closure crossing to the bridge rebuilt its interpreter closure Arc per crossing, so the bridge's HOF cache (keyed on that Arc) recompiled the lambda on every callback; and `col.any`/`col.all` had no native loop. The closure's interpreter form is now built once per object, `col.any`/`col.all` loop natively like `map`/`filter`, and the interpreter's builtin registry is shared rather than cloned per call. tests/w9_language_test.rs pins agreement and that tier-on is never slower than tier-off on the shape |
| `[olang]` `meta.eval` has no time or step budget | workflow rules run user-authored olang through `meta.eval` after the Open AST checker refuses effectful modules, but nothing bounds execution: `(e) => { while true { } }` passes every static check and hangs the http worker and the daily scheduler forever; blocking `time.sleep` as a module only stops the obvious version | **landed** — `meta.eval(src, #{ "max_steps": n, "timeout_ms": ms })` on both the runtime and expansion paths; a budgeted child stays on the interpreter, where every loop iteration, call, and trampolined tail call is charged; `Err("budget exceeded …")` names the bound. Pinned with a `while true`, a recursion, and malformed options |

### Errors that point the wrong way

| Item | Observed | Status |
|---|---|---|
| `[olang]` Parse errors in an imported module blame the importer's `use` line | an invalid declaration inside `lib/m.ol` (`share meta fn`) is reported at the importing file's `use lib.m` — "Undefined variable: share" at `t.ol:1` — with no mention of `lib/m.ol` or the offending line | **landed** — error locations carry the file: an error raised while a module's statements execute names the module's path and line and renders its source, not the importer's `use` line; a module parse error already named the module. Pinned through the binary on both tiers |
| `[olang]` Macro splice errors point at the wrong cause | when a macro's output fails to parse, the error reads "generated source that does not splice as an expression (a trailing // comment in the output is the usual cause)" — the framing and the hint both point away from the real problem (in the observed case, leading-underscore identifiers in the generated code), and the author rewrites the template shape instead of reading the reported line | **landed** — the message leads with the parse error and the generated text, and mentions a trailing `//` comment only when the output actually ends in one |
| `[olang]` Identifiers cannot start with `_` | `let _x = 1` is a parse error ("expected the end of the file" at the underscore — the message does not name the rule). It bites macro authors hardest: `__tmp`-style hygiene prefixes are the cross-language convention for macro-introduced temporaries, and the first `@timed` draft died on it | **landed** — `_tmp`, `__gen0`, `_x` are identifiers; a lone `_` stays the wildcard pattern (the wildcard rule is atomic, so `_ if guard` still reads as a wildcard), and the `spawn`/`meta` literals gained keyword boundaries — `spawn_cost` had started to read as `spawn _cost` the moment `_` could begin a name. language.md and pitfalls.md updated; pinned |
| `[stdlib]` `max`/`min` take a list, not varargs | `max(0, n - 1)` is "Arity mismatch: expected 1, got 2" — the first thing tried for a clamp floor in every language | **landed** — `max(a, b, …)` and `min(a, b, …)` over two or more scalars, the list form unchanged; a lone scalar errors with both spellings named |
| `[olang]` `olang test` runs a file's whole top level, so effectful modules cannot carry tests | adding a `test` block to open-track's `client.ol` made `olang test` execute the file — `mount(...)`, action registrations, a trailing `refresh()` calling `dom.set_timeout`, which raises natively ("the dom module is only available in the browser") and fails the file as "file error". The fix was moving every effect under `if dom.available()`, which the author must know to do | **landed** as the diagnostic — the runner keeps executing the top level (test blocks depend on the declarations around them) and a failure there now prints the statement's file:line:column with the guard convention (`if dom.available()`); the W5 relationship stands: test blocks run only under `olang test`, top-level effects run with them |

### Modules, macros, and the project tool

| Item | Observed | Status |
|---|---|---|
| `[olang]` Libraries could not export macros | the expander resolved a `use` target only as a file relative to the project, never through the shelf, so a `meta fn` in a dependency was invisible to every consumer: `@store` from the web SDK expanded in the served bundle (one flat file) but failed `olang check` and native runs with "no meta fn named 'store'" — for a language whose macro system is a headline feature, a hard wall | **landed** — the expander resolves manifest path and shelf dependencies (`{ path = … }`, `{ shelf = … }`) before the bare shelf name, and a package's macros travel through its `index.ol` re-exports: `share use lib.store { … }` in the index brings lib/store.ol's meta fns along, so `use web` reaches `@store`. The web-sdk chapter's `@store` example is parse-checked by the doc suite against a private shelf |
| `[olang]` Meta fn bodies cannot call imported functions | expansion scope is the file's own declarations plus the pure stdlib — a `share fn` imported with `use` is "Undefined variable" inside a `meta fn` body, so every macro is fully self-contained and the natural factoring (a thin `meta fn` delegating to a tested, shared validator) is forbidden | planned — unchanged; the expansion sandbox stays self-contained per file. The follow-up is bringing pure imported functions into it by resolving through the same module path as the runtime |
| `[olang]` No one-liner evaluation, and probes outside a project cannot see shelf modules | checking a stdlib behavior meant a throwaway file; there is no `olang eval '<expr>'`. Worse, a probe in a scratch directory could not `use markdown` at all — shelf resolution is anchored to the *file's* project root, so a script outside the repo sees only the stdlib | **landed** — `olang eval '<source>'` (REPL semantics, last value printed, the working directory's project libraries in scope), and `olang <file>` outside any project falls back to the working directory's project for dependency and shelf lookups. Pinned |
| `[otc]` `otc new my-lib --lib` scaffolds a package that cannot be imported by the name it prints | a hyphenated name (`open-track-query`) is written into the manifest and printed as `use open-track-query` — which parses as `open` minus `track` minus `query` ("Cannot find module 'open'"). The fix is hand-editing the manifest to `open_track_query`, which nothing says | **landed** — the directory keeps the hyphenated name; the manifest and every printed `use` line carry the identifier (`open_track_query`); a name that cannot be an identifier is refused up front. otc/tests/new_scaffold_test.rs |
| `[otc]` `otc new --web` should scaffold on the web SDK, not the raw stdlib | the `--web` scaffold hand-rolls what the SDK packages: manual static routes, a bespoke `lib/router.ol`, hand-written HTML escaping, raw `db.execute` with no migrations. A fresh project's first real step is deleting most of it (open-track replaced five scaffold files with `serve` and one client file) | **landed** — two files on the SDK (`main.ol`: migrations, rpc routes, `serve`; `client.ol`: `mount`/`action`/`call`, with `btn_confirm` and `dates.stamp`), the manifest depending on the shelf's `web` and `validate`, the wasm copied in when found; `--web-bare` keeps the previous raw-stdlib shape |
| `[otc]` `otc new` inside an existing git repository should say so | the project was created with a `.gitignore` but no `git init` and no word about version control either way | **landed** — outside a repository the next steps end with `git init             # not a git repository yet` |

### Standard library gaps

| Item | Observed | Status |
|---|---|---|
| `[olang]` No path access on maps | `m.a.b` raises "Cannot access field 'a' on non-struct value" — dotted access is for structs — and there is no path helper, so the most-typed expression in open-track is `map_get(map_get(map_get(event, "issue"), "fields"), "estimate")`. Absence-as-Unit already makes each hop safe; it just has to be spelled three times | **landed** — `map_path(m, [k1, k2, …])`: every hop reads like `map_get` (maps, objects, structs, parsed JSON), Unit at the first missing hop, an error when a hop lands on a non-map. Dotted access on maps stays struct-only |
| `[olang]` Collection helpers are split between globals and `col.` with no rule to guess by | `take`, `skip`, `contains`, `filter`, `map`, `fold`, `reverse`, `sort` are globals; `col.any`, `col.all`, `col.sort_by` are prefixed; `drop` does not exist. Writing collection code is guess → "Undefined variable" → the other spelling, several times a session, and it never becomes muscle memory | **landed** — every global list helper is mirrored under `col.` (`col.take`, `col.drop`, `col.map`, …, forwarding to the global), `drop` is `skip`'s alias, and stdlib.md states the rule once: the global spelling is the short form, `col.` always works |
| `[stdlib]` Missing `col.index_of` and `col.slice`; `drop` is not an alias | the index of a value in a list (shift-click range selection) and `take(skip(xs, a), b - a)` for a sub-list were hand-written twice each. `str.last_index_of` was also hand-written — it exists, and was not found, which is a documentation finding | **landed** — `col.index_of(xs, v)` (position or Unit) and `col.slice(xs, from, to)` (half-open, clamped, negative from the end); `drop` as above; `str.last_index_of` surfaced in the stdlib chapter's index |
| `[stdlib]` `validate.check` should accept JsonObject | `json.parse` returns a `JsonObject`; `validate.check` type-gates on `Map` and fails every parsed request body with "expected a map, got JsonObject" — even though `map_get`/`map_has_key`/`map_keys` all accept it. Every server handler needs a copy-to-Map shim (the SDK's `forms.read` accidentally provides one, which hides it in the demo) | **landed** — parsed JSON objects check like Maps in `check`, `ok`, and the `"map"` kind; documented in shelf.md |
| `[stdlib]` `validate.check` has no date kind and no custom predicate | due dates arrived as `["due", "str", #{ "max": 10 }]` because the rule vocabulary knows `str`/`int`/`one_of`/`pattern`/`min`/`max` and nothing about dates; the real check (`dates.parse` succeeds or the value is empty) lives in a wrapper that re-validates one field. Every app with a date field writes the same wrapper | **landed** — a `"date"` kind (ISO day via `dates.parse`, `min`/`max` as ISO days) and `"where": (v) => Result` on any rule; the starter's own test blocks pin both |
| `[stdlib]` A sortable timestamp helper | every table stores `created_at`/`updated_at`; the idiom `to_string(dates.now())` gives `2026-08-31T23:40:06.503309-05:00` — sortable only within one offset, with fractional seconds that are noise in a UI. `dates.utc_now()` helps but nothing says "this is the one to store" | **landed** — `dates.stamp()`: UTC, second precision, `Z` suffix, documented as the storage form |
| `[stdlib]` Browser code cannot ask the page's color scheme | a wasm client rendering an SVG chart must pick a plot theme, but `dom` exposes no `prefers-color-scheme`. open-track ships the light chart and inverts it with a CSS filter in dark mode | **landed** — `dom.prefers_dark()`; the SDK's design tokens also honor an explicit `data-theme` |
| `[stdlib]` `viz` silently ignores unknown spec keys | `viz.chart` forwards only `width`/`height` to plot; a spec written with `w`/`h` renders at the default size with no diagnostic | **landed** — `viz.chart` checks every spec and layer key: `w`/`h` are accepted as `width`/`height`, and an unknown key is an error naming it and the vocabulary |
| `[stdlib]` `viz` charts hardcode SVG text sizes | axis ticks and legend text render at roughly twice the surrounding UI's size, so every chart looks pasted in; there is no font option, and the fix was `svg text { … !important }`, which cannot differ per chart | **landed** — `font_size` and `font` in the spec (and the plot options map), the title and axis labels scaling with the tick text |
| `[stdlib]` plot: line marks have no hover surface | with `"interactive": true`, scatter and bar marks carry their datum as `data-*` attributes, but a line is one `<path>` — a line chart cannot have tooltips on its own. open-track layers a scatter of the same series on top | planned — unchanged; the legend dedupe that makes the scatter-overlay composition clean is in (14b5764) |
| `[stdlib]` plot's y-axis label emitted a malformed SVG transform | `write!` arguments in the wrong order: `fill` received the y-coordinate and the rotate center received the ink color truncated to `#5` — `transform="rotate(-90 16 #5)"`, an SVG parse error in the browser | **landed** (fd1b824, a one-line reorder); the pinned-output test per label branch remains a follow-up |
| `[stdlib]` The shelf `markdown` renderer does not know task lists | `- [ ] write tests` renders as `<li>[ ] write tests</li>`. open-track post-processes the HTML by matching the renderer's exact output, which is fragile and treats descriptions and comments differently | **landed** — `- [ ]` / `- [x]` render as `<li class="task"><input type="checkbox" disabled …>` in source order; pinned in the starter's tests |

### The web SDK

| Item | Observed | Status |
|---|---|---|
| `[web-sdk]` An input's action fires on Enter and again on the change that follows | a `data-action` on a text input runs its handler on the Enter keydown (the "enter" alias) and again on the `change` event the same Enter produces — two invocations milliseconds apart. open-track's "save as…" sent two `searches.save` requests that raced past a delete-then-insert and produced duplicates until a unique index made the slot a database constraint. Any non-idempotent handler (create, append, post) meets the same bug | **landed** — the Enter dispatch records the input; a `change` for the same input within the same beat is the same intent and is not dispatched. Written into the event contract in web-sdk.md |
| `[web-sdk]` `index.ol` exports two different `row`s — the layout one silently wins | the package shares both `lib.sql.row` (single-row query, 3 args) and `lib.ui.row` (horizontal layout, 1 arg). `use web { rows, row }` gets the layout `row`, and the first database lookup dies with "Arity mismatch: expected 1, got 3" far from the import. The SDK's own demo dodges it by importing `lib.sql` directly | **landed** — the query is exported as `one`; `olang check` warns when a module's `share use` lines re-export one name twice (tests/import_alias_test.rs) |
| `[web-sdk]` `serve` sends every asset uncompressed; the 6.7 MB wasm dominates Lighthouse | performance score 0.44: FCP 1.4 s but LCP and TTI at 41.7 s. `olang.wasm` ships as 6,712,922 uncompressed bytes (1.87 MB gzipped, smaller with brotli and `wasm-opt`) and `http.serve` has no `Content-Encoding` support, so an app cannot fix it. It is also served `Cache-Control: no-cache` rather than as an immutable content-hashed URL. ETag/304 revalidation is in (f0da88f) but first load is the cost | **landed** — `Accept-Encoding` negotiation over pre-compressed siblings on disk (`olang_playground.wasm.br` / `.gz`) with `Content-Encoding` and `Vary`; the shell references `/olang.<hash>.wasm`, served `immutable` for a year, with `/olang.wasm` kept as the revalidating fallback (the shim reads the URL off its own script element — `document.currentScript` is null in a module script, which had silently defeated it — and the shell preloads that exact URL). A `wasm32` profile that strips the data stack, and `wasm-opt`, remain follow-ups |
| `[olang]` The whole wasm boot is one synchronous main-thread task | one long task of 5.4 s at 4× CPU throttling (~1.4 s on an M-series laptop): instantiate, parse the 147 KB source bundle, run its top level, mount, first paint, in one call — nothing paints until it ends. `meta.parse` alone is 154 ms natively for the bundle (3,822 lines, 360 top-level nodes); parse plus top level natively is ~400 ms with or without the bytecode tier, so the ~2.3 s wasm boot is the same work ~5× slower. Stripping comments moved nothing — the cost is per node, not per byte | **landed** in part — the shim yields to the browser between instantiating the runtime and starting the session, so the shell paints first, and reports the phases in `window.olangBoot` (`fetch_instantiate_ms`, `session_start_ms`, `total_ms`). Those numbers then showed parsing as the whole remaining cost (instantiate 42 ms, session start 4.5 s, LCP 41.7 s → 20.1 s on the download fix alone): `serve` now pre-expands the bundle on the server (`meta.expand`) so the wasm parser reads it once, and the parser flags macro constructs as it builds rather than serializing the program to JSON to find them. The follow-up — not parsing at all — is the program image (W10): the bundle is parsed once on the server and loaded as bytes |
| `[web-sdk]` `mount` replaces the whole tree on every `apply` | every `apply` re-runs the view over the full store, renders the page to one string, and assigns `innerHTML` — no keyed diff. Visible three ways: LCP keeps moving because each repaint is a new largest-paint candidate; every keystroke island (palette, autocomplete) exists only to dodge the repaint, since a store write destroys the focused input; a chart or long list pays a full render for a toast | **landed** in part — `patch(id, node)` repaints one subtree by id, retiring the imperative-island pattern for the toast, the counter, and the chart; keyed reconciliation over the view tree stays the v2 direction |
| `[web-sdk]` The client bundle is one file | `serve` bundles exactly one client source plus the SDK's browser modules; open-track's client is ~1,000 lines in one file, none of it able to carry test blocks (the bundler strips them) or be imported by server code | **landed** — `"client"` accepts a list of paths, each stripped like the single file and spliced in order after the SDK's modules (`bundle_clients`) |
| `[web-sdk]` `serve`/`dispatch` should accept a logger hook | `dispatch` hardwires `println(method + path + status + ms)`; an app with its own structured logging still has the SDK's unstyled line printed next to it, with no way to silence or restyle it | **landed** — `"log": (req, response, ms) => …` in the serve config (default: the SDK's line); `dispatch_with(routes, req, log)` carries it as a value, since handlers run on worker threads where a cell could not |
| `[web-sdk]` `rows`/`exec` unwrap — no Result-returning variant | `rows(conn, q, params)` unwraps, right for the app's own SQL and wrong for a query built from user text: an FTS5 `MATCH` can fail to parse, and the endpoint must answer an empty list rather than 500. The handler drops to raw `db.query` and re-implements the row mapping | **landed** — `try_rows` and `try_exec` return `Result`s with the same row shape and parameter binding |
| `[web-sdk]` No way to trigger an action programmatically, and no confirm affordance | destructive actions want a confirmation step; with no `confirm()` in the shim and no way to fire an action from code, the workaround is a two-click state-machine button every app reinvents | **landed** — `dom.confirm(message)` in the shim, and `ui.btn_confirm(label, action)`: the view layer arms `confirm:<name>` on the first click and fires `<name>` on the second within a few seconds, so the app registers only the real action |
| `[web-sdk]` The dom shim has no file-input lane | attachments want a file input or drag-a-file; neither is reachable — the event payload carries `id`/`value`/`key`/dataset but never `files`, there is no FileReader bridge, no way to get a picked file's bytes into wasm. Upload shipped through the CLI instead (`ot attach OT-3 file.png`, base64 over rpc). Drop zones and drag events are in the shim already (7e7c35f); the file contents are the missing half | **landed** — `dom.read_file(el, callback)` delivers the first file as `#{ "name", "size", "type", "base64" }` (or `#{ "error": … }`) through the JSON dispatch, in every shim copy and the playground worker |
| `[web-sdk]` No way to ask what currently has focus | a liveness poll that re-syncs when the server's data version moves must never steal an in-progress edit, but `dom` has `focus(el)` and no read side — no active element handle, id, or tag. The workaround is a pile of app-state guards maintained by hand as forms are added | **landed** — `dom.active_id()` |
| `[web-sdk]` web.css should support an explicit theme override | tokens flip only on `prefers-color-scheme`, so an app offering a light/dark/system toggle has to re-own the entire token set with the three-state pattern (`:root` light, `@media dark :root:not([data-theme="light"])`, `:root[data-theme="dark"]`), duplicating every value web.css defines. `viz.tooltip` hardcodes its own dark palette instead of reading the tokens | **landed** — the dark tokens are written once and applied twice (`prefers-color-scheme: dark` unless `data-theme="light"`, and `data-theme="dark"`), so a toggle is one attribute write; `viz.tooltip` reads the design tokens with its old palette as the fallback |
| `[web-sdk]` Delegated events should walk up to the nearest `data-action` ancestor | `eventPayload` read `e.target.dataset` — the exact element clicked — so a button whose label is wrapped in a `span`, or a clickable row with styled children, silently dropped the action | **landed** (7e7c35f: the shim resolves `closest("[data-action]")` bounded by the mount root, `value` stays the target's) |
| `[web-sdk]` Action dispatch re-rendered even when no state changed | `dispatch_action` repainted after every handled event, so a click into a text input carrying `data-action` (Enter-to-submit) wiped the tree, dropping focus and in-progress typing | **landed** (c0508b1: dispatch compares state before and after and repaints only on change). Regression-test candidate; the keyed diff above is the structural fix, since it also preserves focus across genuine repaints |
| `[web-sdk]` Clicks on form controls counted as actions | with `data-action` on a select (save-on-change), the click that opens the dropdown also dispatched the action with pre-change values, and the async repaint snapped the dropdown shut — actively reverting a just-made change | **landed** (ab85402: a click whose target is an input, select, or textarea is a focus/open gesture, never an action; those fire on change and Enter). To be written into the event-contract docs with the Enter/change row above |

### Documentation

| Item | Observed | Status |
|---|---|---|
| `[olang]` Thread-pinned cells deserve a "shared state" chapter | cells are pinned to their creating thread and `http.serve` workers are separate threads, so the natural module-level-cell-as-cache pattern dies at runtime with "cell escaped its thread" (correctly, with a good message pointing at channels). The channel-service pattern that replaces it — one task owns the state, workers talk over channels, rebuilds become single-flight — worked beautifully, but nothing walks the reader from the failing pattern to the working one | **landed** — language.md's concurrency chapter gains "Sharing state under `http.serve`": the channel-service recipe (one owner task, workers ask over channels, rebuilds single-flight) in full |

## W10 — the boot, and what building ledger asked for

Two sources. The boot rows continue W9's web-SDK thread with the
numbers `window.olangBoot` made visible once the download was solved
(2026-09-02): the session start was parsing plus the bundle's top
level, and nothing painted until both were done. The ledger rows come
from planning examples/web/ledger, a daily-use finance application,
against the runtime as it stands — each names an absence met while
designing, not a preference. Rows already open elsewhere that this
work touches stay where they are: keyed reconciliation in `mount`
(W9, partial), the timeline's reach (W6), the W2 number rulings, and
the W8 data/viz cluster. Tags as in W9.

### The boot

| Item | Observed | Status |
|---|---|---|
| `[web-sdk]` The browser parsed the bundle on every visit | after pre-expansion (0.81.0) `session_start_ms` was still parsing plus running; the split on the scaffold app read 22.2 ms parsing against 15.5 ms running the top level — parsing over half of every boot, on every visit, for text that had not changed | **landed** — the program image: `serve` parses the bundle once and encodes it (`meta.encode`; src/olb.rs: the `olb1` header naming the writing version, then the postcard body), serves it at `/app.olb` beside the source, and the shim loads it through `olang_session_start_bin`. Decoding the scaffold's image takes 0.9 ms against 22.2 ms of parsing, and the image is 17.7 KB to the source's 31.4 KB. An image another olang version wrote is refused by name and the shim reads the source instead, so a stale runtime still boots. The image revalidates by ETag like every asset — the HTTP cache is the cross-visit cache, which is why no browser-side store of the parsed program was needed |
| `[web-sdk]` The first paint waited for the wasm | the mount point was empty until instantiate, load, and the top level had all run — LCP 20.1 s at open-track's scale after the download fix | **landed** — `serve` takes `view` and `initial` (a value, or a `() => state` function evaluated per request), renders the view into the mount point on the server, and places the state in a JSON `<script>` (`<` escaped) that the mount point names by `data-olang-state`; `mount` starts the store from it, the server's keys over the caller's defaults, so the first client render reproduces what is on screen and the boot needs no fetch. The view runs on a server worker thread, so it must be a pure function of the state: `confirm_armed` no longer touches its cell without a DOM — the one SDK component that did, found by the scaffold's Delete button. `otc new --web` is wired this way: `lib/pages.ol` holds the view both halves render |
| `[olang]` The wasm carried 612 KB of symbol names | measured by section: Code 5.44 MB, Data 0.68 MB, and a `name` custom section of 0.61 MB. Code by crate: the runtime 2.09 MB, core 0.72, regex 0.73, serde_json 0.32, olang-ods 0.20, pest 0.17, the RSA/bignum/argon2 chain 0.27, toml 0.16 | **landed** — wasm32 builds pass `-C strip=symbols` (.cargo/config.toml): 6,746,426 → 6,266,887 bytes, the image entry point included. The next cuts have their sizes: the browser-side crypto surface (RSA, argon2, ~0.27 MB) and toml (~0.16 MB) behind `native` would be a decision about what the playground promises, not an optimization |
| `[web-sdk]` Boot phases were one number | `session_start_ms` lumped parsing (or decoding) with running the top level and the first render | **landed** — the session result and `window.olangBoot` carry `program` ("image" or "source"), `load_ms`, and `run_ms` |
| `[stdlib]` An http response could not carry bytes without a file | `body_file` was the only binary-safe path; a Bytes value in `body` was stringified | **landed** — a Bytes `body` is served raw, `application/octet-stream` unless a Content-Type is given |
| `[web-sdk]` `action_arg` on a confirm-wrapped action returned the wrong argument | `btn_confirm("Delete", "del:7")` renders `data-action="confirm:del:7"`; `action_arg` took everything after the first colon, so the `del` handler received `"del:7"` and its `parse_int` failed — the scaffold's Delete never worked | **landed** — the `confirm:` prefix is dropped before the argument is read; pinned in view.ol's tests |
| `[olang]` A worker-thread failure said "Runtime error:" three times | the first-paint cell escape printed `Runtime error: Runtime error: Runtime error: cell escaped its thread …` — each layer re-wrapped the message | planned — one prefix, added where the error is reported |

### What building ledger asked for

| Item | Observed | Status |
|---|---|---|
| `[stdlib]` No fixed-decimal formatting | money, percentages, and report columns want `"$1,234.56"`; `format_float` drops trailing zeros and flips to exponent form, and the demo prelude's `money()` gets negatives wrong, so every application hand-rolls the same helper | **landed** — `str.fixed(x, digits)` (exactly `digits` decimals, never exponent form, never `-0.00`; an Int with zero digits keeps its exact digits) and `str.thousands(x, digits)`; the pitfalls chapter has a "`to_string` prints values, not columns" section; the demo prelude's `money()` is negative-safe on `str.fixed` |
| `[stdlib]` A transaction needs an explicit rollback on every failed branch | with `db.begin`/`db.commit`/`db.rollback`, each `Err` arm of a multi-statement write has to roll back by hand; one forgotten arm leaves the database half-written. The SDK's `tx` already wraps this for its own callers | **landed** — `db.transaction(conn, f)`: commit unless `f` returns an `Err` or raises, which roll back; `f`'s result handed through. Lives in the interpreter (only it can call a function value) and works under every tier; the SDK's `tx` is now this call |
| `[stdlib]` Two copies of a migration runner | examples/web/app and examples/web/ledger each carry one, and the SDK's `open_db` is a third | **landed** — `db.migrate(conn, steps)`: the `schema_version` table, one transaction per version, the failing statement named, `Ok(version)`. The SDK's `open_db` and both example stores are callers |
| `[stdlib]` `db.query_one` answers `Ok(())` for no row | "no row" and "a row whose value is unit" are the same answer | **landed** — the ruling: `Ok(())` is the absent shape, unambiguous because a row is always a map (`if r == () => …`); stated in the db table and the help entry, pinned in tests/w10_ledger_test.rs |
| `[stdlib]` `dom.fetch_json` hides the status code | the browser cannot tell a 404 from a 500, so API errors travel in the body — the reason the SDK's envelope exists | **landed** — `dom.request(method, path, body, k)` hands `k` `#{ "status", "headers", "body" }` (status 0 with an `error` when no server answered), over the existing fetch import with a second callback bit; `api.call`/`api.fetch` ride it, so a non-envelope response is `Err(#{ "message": "HTTP 500: …", "status": 500 })` rather than a JSON parse failure in the handler |
| `[stdlib]` ods and viz raise where the language returns Result | every chart needs a non-empty guard around it; a frame built from mixed-type records raises | **landed** — `ods.try_series`, `ods.try_frame_from_records` (the Result forms of the raising constructors), and `viz.check(spec)`: `Ok(spec)` or `Err` naming an unknown key or an empty dataset, so a chart over foreign records is guarded once. `viz.chart` itself still raises, by design: a spec the program wrote is a bug when it is wrong |

### Tools

| Item | Observed | Status |
|---|---|---|
| `[olang]` No snapshot tests | the plot and web-SDK tests compare long HTML and SVG strings inline | planned — `testing.snapshot(name, value)`: written on first run, diffed after, refreshed by a flag |
| `[olang]` `--watch` reruns programs, not tests | `olang --watch` exists for `run`; the test runner has no watch mode | planned — `olang test --watch` |
| `[olang]` The template-escape lints have no fixer | `olang check` warns; the rewrites are mechanical | planned — `olang check --fix` for the lints whose rewrite is unambiguous |

## W11 — the second reading of open-track

Entries added to the open-track improvements document after W9 was
synthesized (2026-09-02 to 2026-09-05), imported on 2026-09-05 with
the evidence as written. Two are already answered by W10 rows and
one did not reproduce; the rest are planned. Tags as in W9.

### Wrong answers and divergence

| Item | Observed | Status |
|---|---|---|
| `[olang]` A `let` inside one `test` block is visible in the next | a test's `let fs = …` replaced the `fs` module for the rest of the file; a test 400 lines later failed with "Cannot access field 'exists' on non-struct value", far from the shadowing block. Reproduced on 2026-09-05 with two blocks | **landed** — a test block runs in its own scope, as a function body does (language.md, "Testing"); `olang check` warns when a `let` anywhere takes a stdlib module's name |
| `[olang]` A call to a name defined in several loaded modules is ~300× slower | a module-level `fn as_str(v)` copied into seven modules cost 0.32 ms per call from inside its own module; renamed to something unique, 3,000 calls take under a millisecond; a name defined in two modules costs 13 µs — resolution walks every module that owns the name on every call. Hidden because each module's tests load one definition | **not reproduced** at HEAD — seven modules each defining `as_str`, 20,000 calls from inside one of them, cost the same as a name defined once (the W9 import-resolution fix is the likely cause of the report). Pinned in tests/w11_test.rs with a 10× bound so a return would be caught |
| `[olang]` `use lib.csv` resolved to the stdlib `csv`, not the project's file | a new `lib/csv.ol` imported as `use lib.csv` produced "Function 'to_records' not found in module" — the qualified prefix `csv.` reached the stdlib module the file shadowed by name, though the `use` named the path explicitly | **landed** — a path import (`use lib.csv`) binds its leaf even over an already-bound stdlib module: the explicit path wins; a one-segment `use csv` is the stdlib module itself. `olang check` says at the `use` that the name now reaches the project's module |
| `[olang]` A raise inside a spawned task is silent, and whoever waits on its channel waits forever | a type error three levels down in a task-owned cache killed the task without a line of output; every later `chan.recv` blocked — the http worker, the test runner, the process — and the deadlock detector named channels, not the exception that emptied them | **landed** in part — a task that ends by raising prints `task olang-spawn-N failed: <error>` on stderr once, as an uncaught error in main would; the `Err` still reaches `task.join`. Closing a dead task's channels and a supervised spawn remain open: channels have no owner to close them by |
| `[web-sdk]` In-process `dispatch` runs handlers on the main thread, so a cell that escapes passes every test | a handler that captured a `cell` passed 311 tests through `web.dispatch` and failed every real request with "cell escaped its thread … olang-http-2": the tests never left the main thread; the workers do | **landed** — under `olang test` (the runner sets `OLANG_TEST`) `dispatch` runs each handler on a task thread and joins it, so a captured cell fails in the test with the server's own message; pinned in server.ol's tests. The static check for cells in handler closures at `http.serve` remains open |

### Errors that point the wrong way

| Item | Observed | Status |
|---|---|---|
| `[olang]` A parameter shadowing an imported function reports the wrong name, and only at call time | `fn gantt_row(s, idx, i, from, span)` shadowed the imported `span`; `olang check` and `olang test` said nothing, and the browser died at boot with "'mount' is an Int, not a function", attributed to the outermost frame — the identifier being called (`span`) and the function containing the call were three frames away | **landed** in part — `olang check` warns at the function: "parameter 'span' of `gantt_row` shadows the function 'span' that this body calls" (imports and module-level functions both count), and an interpreted frame's message names the identifier being called (`'span' is an Int, not a function`) in every call shape, tail calls included. When the shadowing call runs inside a compiled frame the VM does not carry the identifier, and the interpreted caller above still claims the error as its own callee's — the tier's open half |
| `[olang]` A missing builtin inside a dependency is reported at an unrelated line of the caller | the SDK's `open_db` called `db.migrate`, absent from the running binary; the report was "Function 'migrate' not found in module" at `main.ol:20:5` — an `os.exit(2)` in an `if` that had not run — with `open_db` in the call stack. W9 fixed this for parse errors; runtime lookups inside a dependency still blame the importer | **landed** in part — every call-stack frame from another file names it (`open_db (lib/sql.ol)`), so a runtime error inside a dependency is placed by the stack even when the statement it reports is the caller's. The raising expression's own line remains the follow-up |
| `[otc]` A missing shelf entry for one dependency fails every module, blaming another | with `web` declared `shelf = "web"` but absent from the machine's shelf, a module that never imports `web` failed with "Cannot find module 'open_track_query'" — a path dependency that was right there; the resolver stopped at the first unresolvable package and named whatever it was resolving | **landed** — resolution is lenient at run time: every resolvable dependency is handed to the program, a module that never imports the missing package runs, and the `use` that does is told "dependency 'web' is declared in olang.toml but could not be resolved: …names shelf library 'web', which is not on your shelf (register it: `otc lib add <path>`…)" |
| `[olang]` One toolchain lexed `return_focus` as `return` + `_focus` | a fresh machine reported "expected a statement or an expression" at the `_` of `return_focus()`; the 0.81.0 binary and the checkout's build both parse it | **not reproduced** — every keyword rule is atomic with a boundary (`return_kw = @{ "return" ~ kw_end }`), so a keyword prefix never splits an identifier; the reporting machine ran a build from before the boundary rule. `return_focus`, `match_q`, `if_any`, `for_each` and the rest are pinned in tests/w9_language_test.rs |
| `[olang]` A `RuntimeError: unreachable` after an rpc callback returns, with the page living on | opening open-track's palette produced one trap per open after every step of the app's code completed; the page kept working. Reproduces at v0.81.0 and HEAD, image or source boot | **landed** as the diagnostic — the shim now reads the runtime's panic message (`olang_last_panic`, kept by the panic hook) when a call into the wasm traps and prints it before the error propagates, so the next report names the operation. The trap's cause is open until that message is read |

### The web SDK and the browser

| Item | Observed | Status |
|---|---|---|
| `[web-sdk]` `window.olangBoot`: 42 ms to instantiate, 4.5 s to start the session | the download solved, the interpreter read the 220 KB bundle on every visit; TBT 10 s under Lighthouse's throttle | **landed** — the program image and the `load_ms`/`run_ms` split (W10) |
| `[web-sdk]` The shim never used the content-addressed wasm URL | `document.currentScript` is null in a module script, so `data-wasm` fell back to `/olang.wasm` on every page and a preload of the hashed URL downloaded the runtime twice | **landed** (00f3b3c) — the shim finds its own `<script>` by src; the shell preloads the URL the shim fetches |
| `[web-sdk]` `dom.query` raises when nothing matches, so every lookup is a landmine | inside an event handler a raise poisons the wasm instance; one optional lookup ("is there a `<html data-phone>`?") took a whole sidebar down on the platform where the element was absent | **landed** — `dom.find(selector)` answers `()` on a miss and `dom.query_all(selector)` lists every match; `dom.query` keeps its contract for callers that know the element exists |
| `[web-sdk]` `api.call` cannot send a header | a bearer token — the natural shape for the CLI — cannot be used from the client, so an access token rides a cookie set by a server-rendered page: a second mechanism to test and explain | **landed** — `dom.request_with(method, path, body, headers, k)` carries request headers, and `configure(#{ "headers": … })` makes every `call`/`fetch` send them: a bearer token is one shape in the browser and the CLI |
| `[web-sdk]` No idiom for "the row you named is not here" | a handler has `invalid(problems)` for 422 and `Err(msg)` for 500 and nothing between; a missing id came out as a 500 "internal" until twenty sites were rewritten to `error_response(404, …)` by hand | **landed** — `not_found(what)`: the 404 with the row named, beside `invalid`; the chapter says `Err` is for failures |
| `[web-sdk]` `el(tag, attrs)` needs a children list even for void elements | `el("img", #{ "src": … })` fails at render with "Missing required argument: children", a message naming the argument, not the call or the tag | **landed** — `void_el(tag, attrs)` |
| `[web-sdk]` `when(cond, node)` evaluates the node it will not render | as an ordinary function, `when(t != (), span(…map_get(t, "id")…))` reads `t` before deciding and raises when `t` is Unit — exactly when it should say nothing | **landed** — `@when(cond, node)`, a macro in `lib.html` (exported through `use web`): it expands to the `if`, so the node is built only when the condition holds |
| `[web-sdk]` `sdk_dir()` ignores the project's own dependency resolution | `serve` reads web.css and the shim from `WEB_SDK_DIR`, then the shelf, then relative guesses — never from where the project resolved `web` (`olang.lock`); a path dependency runs every module and dies at boot on `./static/web.css` | **landed** — `sdk_dir()` consults, in order, `WEB_SDK_DIR`, the working directory's `olang.lock` (a path entry relative to the manifest, or a shelf entry resolved on this machine), then the shelf and the in-repo guesses; `serve` also takes `"sdk_dir"` outright |
| `[olang]` `http.serve` binds 127.0.0.1 only | there is no way to listen on a LAN interface; the honest instruction for use beyond the laptop is a reverse proxy or an SSH tunnel | **landed** — the `"bind"` option (`http.serve(port, handler, #{ "bind": "0.0.0.0" })`), default unchanged; the SDK's `serve` passes its own `"bind"` through |

### Tools and the standard library

| Item | Observed | Status |
|---|---|---|
| `[otc]` The lockfile pins shelf and SDK dependencies by absolute machine path | `olang.lock` records `kind = "path"` with `/Users/<me>/.olang/shelf/validate` and `/Users/<me>/Code/olang/frameworks/web-sdk`; a clone elsewhere resolves nothing the lock names, and the first message names whichever package resolution reached first | **landed** — a shelf dependency locks as `kind = "shelf"` with its name, resolved through each machine's own shelf (older path-form entries are honored while they agree); path entries were already the manifest's relative path (`otc add` records them relative to the root). A locked path or shelf entry that this machine does not have re-resolves instead of failing, and `otc install` says which entry it is re-resolving |
| `[stdlib]` `dates.stamp()` is second-precision, so it cannot be a row's version | optimistic concurrency compared `updated_at` stamps and two edits inside one second looked identical (a stale write passed as 200 instead of 409); an activity feed ordered by the same stamps lost the order within a second | **landed** — `dates.stamp_ms()` (UTC, milliseconds, `Z`); the dates table says which stamp to key concurrency on |


One lane at a time, each landing with its tests and documentation in
the same change. When an item's cost proves larger than expected, the
schedule moves, not the bar. New findings from use join the tables
with their evidence; nothing enters on speculation.
